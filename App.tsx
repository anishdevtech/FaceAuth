/**
 * FaceAuth — Offline Facial Recognition System
 * Hackathon 7.0 — NHAI Datalake 3.0 Integration
 *
 * Day 1: Camera test screen to validate native stack
 */

import React from 'react';
import CameraTestScreen from './src/screens/CameraTestScreen';

const App: React.FC = () => {
  return <CameraTestScreen />;
};

export default App;
